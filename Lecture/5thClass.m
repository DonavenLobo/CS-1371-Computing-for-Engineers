%% Vectors
%
% Collections of data must be homogenous (i.e. all entries must be of the same primative)
% Variables can contain collections of data
% (rows x columns)
% Vectors have to be (1 x N) hence one dimentional
% If a collection is (M x N) where M > 1, then the collection can be called
% an array
% A 1 x 1 vector is refered to as a Scalar
% The first entry of a vector has the index position 1

%% New functions

% class(var) --> Returns what data type a variable is holding
% islogical(var) --> Gives a logical output based on variables data type (True if variable is logical)
% ischar(var) --> True if variable is Char
% isnumeric(var) --> True if variable if Double
% sort() --> sorts vectors in increasing order

%% Numeric Vector functions

% min() --> Gives you the minimum value in the vector
% max() --> Gives you the maximum value in the vector
% sum() --> Adds all the entries of the vector
% prod() --> Multiplies all entries of the vector
% mod() --> Modulus function works with vectors of same size
% mean() --> Gives you the mean of all the value of entries

%% Char Vector functions

% strcmp() --> Compares to strings and tells you if they are equal or not
% strtok() --> Stores selected parts of strings
% strfind()--> Finds the position of a string in the original string
% strrep() --> Finds and replaces substrings


