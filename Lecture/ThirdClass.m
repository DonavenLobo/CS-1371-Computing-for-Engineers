% More notes in the drive folder
%% Bases
% Base 10 - Decimal - Normal human counting system
% Base 2 - Binary
% Base 16 - Hexadecimal

% 8 bits --> 1 byte
% 4 bits --> nibble

% To distinguish charectors you have to put the value in quotes. e.g. 'ABC'

% ASCII Table is a look up table that assigns decimal, binary etc. values
% to different charecters

% Scope - Variables assigned in a function only exist inside the function

% uint8()  - unsigned integer 8-bits -- can use this to find ascii values 